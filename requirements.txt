# Библиотеки, которые нужны проекту.
# Установить все разом:   pip install -r requirements.txt

# --- Telegram-бот ---
# aiogram — современная библиотека для ботов (версия 3).
aiogram==3.13.1

# --- Веб-панель администратора ---
# FastAPI — лёгкий и быстрый веб-фреймворк (на нём сделана админка).
fastapi==0.115.0
# uvicorn — сервер, который запускает FastAPI-приложение.
uvicorn==0.30.6
# jinja2 — шаблоны HTML (рисуем страницы панели).
jinja2==3.1.4
# python-multipart — нужен FastAPI, чтобы принимать данные из HTML-форм.
python-multipart==0.0.12

# --- Планировщик задач ---
# APScheduler — раз в час проверяет, у кого заканчивается подписка, и шлёт напоминания.
APScheduler==3.10.4

# --- Генерация ключей WireGuard / AmneziaWG ---
# cryptography умеет считать ключи X25519 (это и есть ключи WireGuard),
# поэтому утилита `wg` для генерации ключей не обязательна.
cryptography==43.0.1

# --- Платежи через ЮKassa (опционально, для авто-приёма СБП) ---
# Можно не ставить, если используешь ручное подтверждение. Оставлено для будущего.
yookassa==3.5.0

# --- Удобная загрузка .env-файла с секретами ---
python-dotenv==1.0.1
